{
  "key": "organizations",
  "title": "Организации",
  "fields": [
    {
      "key": "name",
      "label": "Организация",
      "type": "text",
      "required": true
    },
    {
      "key": "id",
      "label": "ID",
      "type": "integer",
      "required": true,
      "readOnly": true,
      "source": "backend"
    },
    {
      "key": "status",
      "label": "Статус",
      "type": "select",
      "required": true,
      "options": ["Одобрено", "Возвращено", "На рассмотрении", "Черновик"]
    }
  ]
}
